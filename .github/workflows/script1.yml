name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: eccho "Hey everyone"
      - name: multiline script
        run: |
          node -v
          npm -v
          






